interface:
  display_name: "ADR"
  short_description: "Write readable, evidence-backed ADRs"
  default_prompt: "Use $adr to draft or revise an ADR with context, approach, component pseudocode, decisions, and consequences."
