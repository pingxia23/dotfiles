interface:
  display_name: "Code Implement Loop"
  short_description: "Implement, review, commit, and run branch review"
  default_prompt: "Use $code-implement-loop to implement the provided plan, run its review flow, commit the changes, and run full-branch review unless the skip flag is supplied."
