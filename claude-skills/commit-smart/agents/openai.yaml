interface:
  display_name: "Commit Smart"
  short_description: "Safe commit, push, PR, and Codex trigger workflow"
  default_prompt: "Use $commit-smart to run tests, commit safely, push, create or reuse the PR, and best-effort post a fresh `@codex review` comment."
