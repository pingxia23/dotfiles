interface:
  display_name: "Commit Smart"
  short_description: "Safe commit, push, and PR workflow"
  default_prompt: "Use $commit-smart to run tests, commit safely, push, and create a draft PR only if one does not already exist."
