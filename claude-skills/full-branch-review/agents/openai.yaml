interface:
  display_name: "Full Branch Review"
  short_description: "Review a PR branch and publish findings inline"
  default_prompt: "Use $full-branch-review to preflight and review the current PR branch, then publish each finding inline and return the raw structured result."
