{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["approve", "revise"],
      "description": "approve if the plan is sound; revise if it has concrete issues that should be addressed before user review"
    },
    "comments": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of review comments. Empty array when verdict is approve."
    }
  },
  "required": ["verdict", "comments"],
  "additionalProperties": false
}
